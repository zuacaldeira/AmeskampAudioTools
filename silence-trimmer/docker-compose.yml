services:
  app:
    build: .
    container_name: silence-trimmer-app
    restart: unless-stopped
    ports:
      - "127.0.0.1:5000:5000"
    tmpfs:
      - /tmp:size=2G
    environment:
      - TMPDIR=/tmp
